[package]
name = "password_manager"
version = "0.1.0"
edition = "2021"

[dependencies]
diesel = { version = "1.4", features = ["postgres", "chrono"] }
dotenv = "0.15"
tokio = { version = "1", features = ["full"] }